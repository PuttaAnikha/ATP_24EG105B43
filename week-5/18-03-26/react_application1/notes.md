REACTJS
-first thing is when we create a react application the folder name must not contain capital letters and if they are multi words just use hiphens no spaces are allowed

# to create react application
->after creating the folder open terminal and enter the command   "npm create vite@latest"
   -after this, ok to proceed?y
   -project name:since we alrdy have the folder for the application just enter the (.)
   -framework:react
   -variant-JvaScript
   install:yes

-the modern aplications are the single page applications
-react is a 'component based architecture'.
-components should be split as small as possible.
-if the extension is jsx it is a component.
-app.jsx is the root component of the application
-<div>
    <h1>Welcome to react</h1>
    <h2>username:{username}</h2>
    <h2>{person.name}</h2>
  </div>
  ->this is not html code it is a jsx code


